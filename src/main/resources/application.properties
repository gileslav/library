# podesavanja servera ukoliko je potrebno vise od jedne spring boot aplikacije pokrenuti
# server.port = 8090

# ovaj deo ispod se koristio u kombinaciji sa JSP stranicama, to i welcome kontroler se kombinuje ako je potrebno ponovo
# iste ukljuciti!
#spring.view.prefix: /WEB-INF/jsp/
#spring.view.suffix: .jsp

# ===============================
# = DATA SOURCE
# ===============================

spring.datasource.url=jdbc:mysql://localhost:3306/libraryDB
spring.datasource.username=root
spring.datasource.password=darko

# ===============================
# = JPA / HIBERNATE
# ===============================

# Prikazuj SQL upite koje JPA generise
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

spring.jpa.properties.connection.CharSet=utf8
spring.jpa.properties.connection.characterEncoding=utf8
spring.jpa.properties.hibernate.connection.useUnicode=true